<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="ProiectTry.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:ProiectTry"
    Shell.FlyoutBehavior="Disabled">

    <TabBar>
        <ShellContent Title="Inchirieri Echipament Alpin" ContentTemplate="{DataTemplate local:MainPage}" />
        <ShellContent Title="Despre" ContentTemplate="{DataTemplate local:AboutPage}" />
        <ShellContent Title="Echipamente" ContentTemplate="{DataTemplate local:EchipamentEntryPage}" />
    </TabBar>

</Shell>
